import { LimitProjectContentsPipe } from './limit-project-contents.pipe';

describe('LimitProjectContentsPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitProjectContentsPipe();
    expect(pipe).toBeTruthy();
  });
});
